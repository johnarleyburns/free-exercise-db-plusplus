# v0.8 — release stabilization

Adds deterministic-build support via `SOURCE_DATE_EPOCH`, byte-for-byte reproducibility verification, release-contract/invariant tests, methodology documentation, versioning policy, release checklist, and a workflow patcher.

No muscle-role semantics or 1.0/0.5/0.0 set-credit changes.
