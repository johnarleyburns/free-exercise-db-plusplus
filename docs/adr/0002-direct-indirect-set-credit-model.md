# ADR 0002: DB++ set-credit model

Status: accepted

The project default is direct `1.0`, indirect `0.5`, and stabilizer `0.0`.
This is an analysis convention, not a claim that adaptation is linear. Other
models may be computed by applications without changing the database contract.
