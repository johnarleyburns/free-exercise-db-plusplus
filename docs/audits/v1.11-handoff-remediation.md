# v1.11 Handoff Remediation Baseline

Audit started: 2026-08-27

This ledger records the baseline for completing the unresolved requirements in
`HANDOFF.md` before beginning the corrected v1.12 Swift SPM hardening scope.
Python remains the semantic oracle. The existing `v1.11.0` tag is immutable;
any corrective release must use a new patch version.

## Baseline

| Field | Value |
| --- | --- |
| Branch | `main` |
| HEAD | `6d11bd8` (`release: record post-release smoke`) |
| `origin/main` | `6d11bd8` |
| Existing release tag | `v1.11.0` (must not be moved) |
| Python package version | `1.11.0` |
| Relationship artifact project release | `1.5.1` (meaning requires metadata audit) |
| Swift package tools version | Swift 6.0 |
| Untracked user file | `HANDOFF.md`; preserved and not staged by this part |
| Working-tree documentation change | `current_status.md`; points to this plan's Phase 0 / Part 0A |

## Read scope

Part 0A inspected the handoff and roadmap, current status, release and
capability documentation, the existing v1.11 audit, Python public engine
modules, Swift package sources and tests, schemas/resources, cross-language
fixtures, consumer scripts, and GitHub workflow definitions.

The relevant Python oracle surface is:

```text
database.py
workout.py
plan.py
target.py
analysis.py
_analysis/
longitudinal.py
relationships.py
training.py
plan_evaluation.py
training_state.py
progression.py
planning.py
coaching.py
intent.py
```

The relevant Swift surface is:

```text
CoreDomain.swift
FreeExerciseDBPlusPlus.swift
PlanAnalysis.swift
PlanEvaluation.swift
PlanGeneration.swift
Progression.swift
Coaching.swift
TrainingEngine.swift
WorkoutIntent.swift
```

## Known findings requiring remediation

| ID | Handoff part | Area | Severity | Python behavior | Current Swift/repository behavior | Required fix | Regression | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BASE-001 | AY, BY, CB | Generation parity | blocker | Python golden output is the reference for generated plan, evaluation, rationale, and provenance | Direct comparison reported envelope, allocation, rationale, and provenance mismatches | Aligned Swift allocation, rationale, envelope, and provenance; exact comparator now passes | `scripts/test_swift_python_parity.sh` generation comparison | fixed — `38e6215` |
| BASE-002 | AY, BY, CB | History parity | blocker | Python emits complete history/state semantics | Direct comparison reported cycle, future-workout, adherence, naming, and missed-occurrence differences | Aligned active-plan occurrence state, offsets, missed rows, prescription identity, and provenance | `scripts/test_swift_python_parity.sh` history comparison | fixed — `38e6215` |
| BASE-003 | AY, BY, CB | Adaptation parity | blocker | Python emits complete decisions, changes, evidence, and proposed-plan semantics | Direct comparison reported incomplete change records, decision identity, evidence, and provenance | Aligned progression/hold decisions, full before/after changes, revision IDs, and result provenance | `scripts/test_swift_python_parity.sh` adaptation comparison | fixed — `38e6215` |
| BASE-004 | AX, AY | Fixture coverage | blocker | Reference behavior must be exercised across the required adversarial matrix | Evaluation, history, generation, and adaptation each had only one case | Added partial ranges, custom credits, constraints, arbitrary cycles, future exclusion, locks, maximum rejection, regeneration, substitution, and individual progression cases | `scripts/test_cross_language_fixtures.sh`; 3/2/4/2/2 operation matrix cases plus 11 intent cases | fixed — `38e6215` |
| BASE-005 | AY | Swift parity harness | blocker | Every supported operation is compared to committed Python expected output | Swift tests did not compare full history/generation/adaptation/intent envelopes | Added exact canonical comparisons for evaluation, history, progression, generation, adaptation, intent resolution, and intent generation with first-path diagnostics | `scripts/test_swift_python_parity.sh` | fixed — `38e6215` |
| BASE-006 | AZ, BO, CB | External consumer | blocker | Packaged consumer demonstrates the complete public workflow | Consumer previously checked presence and omitted full intent-driven golden comparison | External SPM consumer now calls all required public operations and compares every representative output to Python goldens | `scripts/test_swift_consumer.sh` | fixed — `38e6215` |
| BASE-007 | BA | Indexing | high | Immutable lookup indexes are built once and cover all required dimensions | Existing indexes were not cached inside `TrainingEngine`; family membership remained scan-based; movement-pattern indexing used the wrong source field | Added immutable TrainingEngine indexes, family-member index, and canonical annotation-pattern lookup | Index and concurrent-read tests plus generation parity | fixed — `38e6215` |
| BASE-008 | BJ | Audit evidence | high | Audit ledger records each finding and its fix | Existing audit was a short prose claim and did not record direct parity failures | This ledger now records the operation, prior behavior, fix, regression, and fixing commit; final zero-gap evidence is appended below | Ledger review and full verification pass | fixed — `38e6215` |
| BASE-009 | BX | Release sequencing | high | Normal CI must pass on exact release-preparation human SHA before tagging | The immutable v1.11.0 tag predates the correction and cannot prove the new checks | Prepared a corrective v1.11.1 release without moving v1.11.0; the normal CI gate remains the tag prerequisite | v1.11.1 release-preparation audit and immutable-tag check | fixed for preparation — `38e6215` |
| BASE-010 | BN | Wheel acceptance | high | Installed wheel must exercise all required APIs against goldens | Existing evidence did not demonstrate the complete installed API list | Added a fresh-venv wheel acceptance that verifies installed import location, resources, all five APIs, and goldens | `scripts/test_python_wheel_acceptance.sh` | fixed — `38e6215` |
| BASE-011 | BD | Roadmap | high | v1.12 is Swift packaging/app-readiness; Kotlin/R are later | Later roadmap sections retained stale v1.12 engine/parity claims | Rewrote stale v1.12/Kotlin/R language and gate requirements | Roadmap match audit; no stale schedule assertions remain | fixed in Part 1A |
| BASE-012 | BV | Metadata | medium | Release/project/resource metadata has explicit compatible meanings | Relationship artifact `projectRelease` was ambiguous in the audit | Documented that it identifies the independently versioned artifact's introduction release; retained it, refreshed stale canonical-artifact checksums, and prepared v1.11.1 metadata | SHA-256 checks, version audit, `docs/releases/v1.11.1.md` | fixed — `38e6215` |

## Initial verification evidence

- `git status --short` showed only the preserved untracked `HANDOFF.md` before
  this ledger and the requested `current_status.md` pointer change.
- `origin/main` and local `main` both resolved to `6d11bd8`.
- `v1.11.0` exists and must not be rewritten.
- Cross-language directories currently contain one case each for evaluation,
  history, generation, and adaptation, and four progression cases.
- The Swift consumer script does not call `generatePlanFromIntent`.
- The workflow includes Swift, Python wheel, Kotlin, and R checks, but the
  acceptance requirements still need exact operation-level parity coverage.
- `git diff --check` passed after the documentation/status update.

## Part 1A completion evidence

- `ROADMAP.md` now states v1.11 Python/Swift parity, v1.12 Swift SPM
  hardening, v1.13 Kotlin, and v1.14 R.
- The v1.12 objective, examples, fixtures, CI, release artifacts, and release
  gate are scoped to Swift SPM hardening and retained Python/Swift parity.
- Kotlin and R implementation requirements were removed from the v1.12 gate;
  existing Kotlin/R checks remain compatibility checks only.
- A repository-wide roadmap scan found no stale assertions for Kotlin/R parity,
  v1.12 engine completion, or adaptive coaching completion in v1.12.
- `git diff --check` passes.

## Phase 2 progress

Part 2A implementation is present: `TrainingEngine` now retains immutable,
deterministically sorted indexes for exercise ID, muscle, movement pattern,
equipment, and family members. Database movement-pattern lookup now uses
canonical annotation patterns, with focused database regressions.

The external consumer now invokes `generatePlanFromIntent` and continues to
pass outside-repository compilation and execution. Full Phase 2 is not marked
complete until the remaining fixture expansion, exact Swift/Python comparisons,
history/generation/adaptation repairs, wheel acceptance, and metadata audit
are complete.

## Final Phase 2 zero-gap verification

The remaining Phase 2 work is closed by commit `38e6215`.

| Area | Evidence | Result |
| --- | --- | --- |
| Evaluation, history/state, progression, generation, adaptation | `scripts/test_swift_python_parity.sh` compares committed Python expected JSON with path-level diagnostics | pass |
| Intent resolution and intent-driven generation | Same harness compares all 11 intent fixtures, including history-aware and partial-target cases | pass |
| Fixture authoring and repeatability | `scripts/test_cross_language_fixtures.sh` regenerates every JSON artifact and checks hashes | pass |
| External Swift consumer | `scripts/test_swift_consumer.sh` builds in a temporary SPM package, invokes all public operations, and compares goldens | pass |
| Python distribution | `scripts/test_python_wheel_acceptance.sh` builds/installs a wheel in a fresh venv, verifies import location, and compares all five APIs | pass |
| Swift package regression suite | `swift test --package-path packages/swift/FreeExerciseDBPlusPlus` | 42 passed |
| Release metadata | v1.11.1 preparation doc, package version, immutable v1.11.0 tag, artifact-version explanation, and refreshed canonical hashes | pass |

The relationship artifact's `metadata.projectRelease` remains `1.5.1` by
design: it identifies the independent artifact's introduction release. It is
not the current project release and must not be rewritten during v1.11.1
preparation. The release workflow now requires successful normal CI for the
exact tagged commit before publication.

### Historical probe note

The earlier probe findings in the handoff were intentionally preserved as
historical context. They were resolved by the exact comparisons recorded in
the final verification table above; no field normalization or comparison
weakening was used.

Parts 0A and 1A remain complete, and Phase 2 is now closed. The next roadmap
work is v1.12 Swift SPM production hardening; Kotlin and R engine parity remain
deferred to their documented releases.
