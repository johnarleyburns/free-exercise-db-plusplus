# v1.11 Handoff Remediation Baseline

Audit started: 2026-08-27

This ledger records the baseline for completing the unresolved requirements in
`HANDOFF.md` before beginning the corrected v1.12 Swift SPM hardening scope.
Python remains the semantic oracle. The existing `v1.11.0` tag is immutable;
any corrective release must use a new patch version.

## Baseline

| Field | Value |
| --- | --- |
| Branch | `main` |
| HEAD | `6d11bd8` (`release: record post-release smoke`) |
| `origin/main` | `6d11bd8` |
| Existing release tag | `v1.11.0` (must not be moved) |
| Python package version | `1.11.0` |
| Relationship artifact project release | `1.5.1` (meaning requires metadata audit) |
| Swift package tools version | Swift 6.0 |
| Untracked user file | `HANDOFF.md`; preserved and not staged by this part |
| Working-tree documentation change | `current_status.md`; points to this plan's Phase 0 / Part 0A |

## Read scope

Part 0A inspected the handoff and roadmap, current status, release and
capability documentation, the existing v1.11 audit, Python public engine
modules, Swift package sources and tests, schemas/resources, cross-language
fixtures, consumer scripts, and GitHub workflow definitions.

The relevant Python oracle surface is:

```text
database.py
workout.py
plan.py
target.py
analysis.py
_analysis/
longitudinal.py
relationships.py
training.py
plan_evaluation.py
training_state.py
progression.py
planning.py
coaching.py
intent.py
```

The relevant Swift surface is:

```text
CoreDomain.swift
FreeExerciseDBPlusPlus.swift
PlanAnalysis.swift
PlanEvaluation.swift
PlanGeneration.swift
Progression.swift
Coaching.swift
TrainingEngine.swift
WorkoutIntent.swift
```

## Known findings requiring remediation

| ID | Handoff part | Area | Severity | Python behavior | Current Swift/repository behavior | Required fix | Regression | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BASE-001 | AY, BY, CB | Generation parity | blocker | Python golden output is the reference for generated plan, evaluation, rationale, and provenance | Direct comparison reports mismatches including supported-exercise count, set ranges, effective credits, session counts, rationale, and provenance | Compare every generation field and make Swift match Python | Shared generation fixtures and full parity harness | open |
| BASE-002 | AY, BY, CB | History parity | blocker | Python emits complete history/state semantics | Direct comparison reports missing `nextScheduledOccurrence`, cycle-position, adherence, naming, and missed-occurrence differences | Match active-plan, window, offset, adherence, missingness, and canonical encoding behavior | Shared history fixtures | open |
| BASE-003 | AY, BY, CB | Adaptation parity | blocker | Python emits complete decisions, changes, evidence, and proposed-plan semantics | Direct comparison reports missing `exerciseId`, prescription fields, differing change types, decisions, reason codes, and evidence | Match the complete adaptive result envelope and decision semantics | Shared adaptation fixtures | open |
| BASE-004 | AX, AY | Fixture coverage | blocker | Reference behavior must be exercised across the required adversarial matrix | Evaluation, history, generation, and adaptation each currently have only one input/expected/metadata case; progression has four cases | Add the required fixture families and deterministic regeneration | Fixture regeneration twice with no diff | open |
| BASE-005 | AY | Swift parity harness | blocker | Every supported operation is compared to committed Python expected output | Swift tests compare evaluation/progression expectations, but history/generation/adaptation tests do not compare full Python goldens | Make all operation tests perform canonical JSON comparisons with path-level diagnostics | Swift parity test target | open |
| BASE-006 | AZ, BO, CB | External consumer | blocker | Packaged consumer demonstrates the complete public workflow | `scripts/test_swift_consumer.sh` called `generatePlan`, not `generatePlanFromIntent`, and checked presence rather than semantic equality | Consumer now invokes `generatePlanFromIntent`; golden comparison remains open | Isolated SPM consumer | partially fixed in Phase 2 |
| BASE-007 | BA | Indexing | high | Immutable lookup indexes are built once and cover all required dimensions | Existing indexes were not cached inside `TrainingEngine`; family membership remained scan-based; movement-pattern indexing used the wrong source field | Added immutable TrainingEngine indexes, family-member index, and canonical annotation-pattern lookup | Index and concurrent-read tests | fixed in Phase 2A; generator integration audit remains open |
| BASE-008 | BJ | Audit evidence | high | Audit ledger records each finding and its fix | Existing audit is a short prose claim and does not record the direct parity failures | Replace with operation-by-operation finding records and repeat until one full pass has zero gaps | Audit ledger review plus final zero-gap pass | open |
| BASE-009 | BX | Release sequencing | high | Normal CI must pass on exact release-preparation human SHA before tagging | Existing tag/release history does not prove normal CI on the exact release-preparation SHA | Use a new corrective release if needed and enforce exact-SHA normal CI before tagging | CI run association and remote audit | open |
| BASE-010 | BN | Wheel acceptance | high | Installed wheel must exercise all required APIs against goldens | Existing isolated-wheel workflow/manual evidence does not demonstrate the complete required API list, especially `generate_plan_from_intent` | Add full installed-wheel acceptance outside checkout | Wheel smoke script/job | open |
| BASE-011 | BD | Roadmap | high | v1.12 is Swift packaging/app-readiness; Kotlin/R are later | Later roadmap sections retained stale v1.12 engine/parity claims | Rewrote stale v1.12/Kotlin/R language and gate requirements | Roadmap match audit; no stale schedule assertions remain | fixed in Part 1A |
| BASE-012 | BV | Metadata | medium | Release/project/resource metadata has explicit compatible meanings | Relationship artifact still reports `projectRelease` `1.5.1`; fixture and package metadata require reconciliation | Audit and correct only fields whose meaning requires correction; document artifact versions | Metadata and checksum checks | open |

## Initial verification evidence

- `git status --short` showed only the preserved untracked `HANDOFF.md` before
  this ledger and the requested `current_status.md` pointer change.
- `origin/main` and local `main` both resolved to `6d11bd8`.
- `v1.11.0` exists and must not be rewritten.
- Cross-language directories currently contain one case each for evaluation,
  history, generation, and adaptation, and four progression cases.
- The Swift consumer script does not call `generatePlanFromIntent`.
- The workflow includes Swift, Python wheel, Kotlin, and R checks, but the
  acceptance requirements still need exact operation-level parity coverage.
- `git diff --check` passed after the documentation/status update.

## Part 1A completion evidence

- `ROADMAP.md` now states v1.11 Python/Swift parity, v1.12 Swift SPM
  hardening, v1.13 Kotlin, and v1.14 R.
- The v1.12 objective, examples, fixtures, CI, release artifacts, and release
  gate are scoped to Swift SPM hardening and retained Python/Swift parity.
- Kotlin and R implementation requirements were removed from the v1.12 gate;
  existing Kotlin/R checks remain compatibility checks only.
- A repository-wide roadmap scan found no stale assertions for Kotlin/R parity,
  v1.12 engine completion, or adaptive coaching completion in v1.12.
- `git diff --check` passes.

## Phase 2 progress

Part 2A implementation is present: `TrainingEngine` now retains immutable,
deterministically sorted indexes for exercise ID, muscle, movement pattern,
equipment, and family members. Database movement-pattern lookup now uses
canonical annotation patterns, with focused database regressions.

The external consumer now invokes `generatePlanFromIntent` and continues to
pass outside-repository compilation and execution. Full Phase 2 is not marked
complete until the remaining fixture expansion, exact Swift/Python comparisons,
history/generation/adaptation repairs, wheel acceptance, and metadata audit
are complete.

## Part 0A completion condition

Part 0A and Part 1A are complete. The next implementation part is Phase 2 /
Part 2A: build immutable deterministic Swift TrainingEngine indexes while
preserving the untracked user-provided `HANDOFF.md`.
