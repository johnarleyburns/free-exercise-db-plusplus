# v0.8 — release stabilization

This ready-to-copy package is built from the current public repository state.

Changes:
- fixes deterministic timestamp generation with `SOURCE_DATE_EPOCH`;
- adds byte-for-byte reproducibility CI verification;
- adds a release-contract invariant test;
- changes the JSON Schema `$id` from the placeholder URL to the canonical repository URL;
- updates the current GitHub Actions workflow directly;
- replaces the stale README with documentation for schema 0.3.0 / converter 0.8.0;
- adds methodology, compatibility/versioning policy, and a release checklist.

No exercise muscle mappings or set-credit semantics are changed.
