# v0.9.0 — 1.0 release candidate infrastructure

This batch freezes and tests the intended 1.0 consumer contract.

Adds:
- upstream ID compatibility detection;
- golden semantic mapping regression tests;
- explicit policy test for all remaining medium-confidence mappings;
- canonical `workout.schema.json` URL;
- `COMPATIBILITY.md`;
- consolidated `EVIDENCE.md`;
- tag-driven GitHub release workflow with SHA-256 checksums;
- release protection against upstream changing between the reviewed build and the tag.

No set-credit semantics change.
