# Mapping contract

Structural mappings retain the existing field registry shape. Identity crosswalks use `exercise-interop-mapping.schema.json` because an exercise identity relation is not a field transform. Records retain target specification, external identifier/name, relation, direction, confidence, rationale, and source reference.
