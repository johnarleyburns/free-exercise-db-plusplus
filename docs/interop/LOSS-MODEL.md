# Mapping loss model

Converters should emit `informational`, `lossy`, `unsupported`, or `invalid`. A lossy result must include each affected source path and a reason; unsupported fields must never disappear silently. The contract is in [mapping-loss.schema.json](../../mapping-loss.schema.json).
