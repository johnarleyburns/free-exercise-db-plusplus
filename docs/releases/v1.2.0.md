# Free Exercise DB++ v1.2.0

v1.2 provides audited interoperability capability mappings, a reviewed Android Health Connect identity crosswalk, deterministic coverage reports, loss semantics, and the Python mapping lookup API. It does not provide operational import/export; that is v1.3 scope.
